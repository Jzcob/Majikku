package me.majikku.majikku.Files;

import me.majikku.majikku.Majikku;
import org.bukkit.configuration.file.FileConfiguration;
import org.bukkit.configuration.file.YamlConfiguration;

import java.io.File;
import java.io.IOException;

public class SpawnFile extends AbstractFile {

    /*Majikku plugin;
    public SpawnFile(Majikku plugin) {this.plugin = plugin;}

    File file = new File(plugin.getDataFolder()+"/spawn.yml");
    public FileConfiguration fileConfiguration = YamlConfiguration.loadConfiguration(file);

    public void saveYML(FileConfiguration fileConfiguration, File file) {
        try {
            fileConfiguration.save(file);
        } catch (IOException e) {
            e.printStackTrace();
        }

        saveYML(fileConfiguration, file);

        fileConfiguration.getString("path");

    }*/


}